server.port: 9100
spring:
  application.name: authms
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/auth_db
    username: auth_db
    password: Shahin@12345
    initialization-mode: always
  sql.init.mode: always
